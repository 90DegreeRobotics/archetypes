# Plan: Implement Lane 0 Spine — 2026-07-13 21:32

## Status
COMPLETED (post-audit corrections continued in `plan_2026-07-13_2147_lane-0-spine-audit-fix.md`)

## Goal
Implement the Lane 0 (Spine) contract for the Archetypes multilane build. This lays down the `GameMode` framework, services layer (`llm`, `chronos`, `paths`, `ledger`), and foundational shared types (`Difficulty`, extended `WitnessProfile`), while fixing canon alignment. This is the prerequisite before Lanes A, B, and C can begin building their specific gameplay mechanics.

## Steps
### Step 1 — Fix the Lane 0 Contract in CODEX_LANE_0_SPINE.md
- [x] Action: Update `CODEX_LANE_0_SPINE.md` to point to `crates/engine/src/chamber/boot.rs` instead of `boot.rs`, and include `chamber/council.rs`, `chamber/ritual.rs`, `main.rs`, and `Cargo.toml`.
- Files touched: `docs/ledger/2026/07/CODEX_LANE_0_SPINE.md`
- Expected outcome: The contract accurately reflects the files Lane 0 will modify.

### Step 2 — Implement Mode Framework and GameMode Selector
- [x] Action: Create the mode framework under `crates/engine/src/modes/`. Replace the hardwired `Standard Mode` button in `chamber/boot.rs` with a `GameMode` selector. Add mode registration and basic transition logic. Add tests for mode registration.
- Files touched: `crates/engine/src/modes/mod.rs` (new), `crates/engine/src/chamber/boot.rs`, `crates/engine/src/main.rs`.
- Expected outcome: The game can route into different modes from the main menu.

### Step 3 — Extract and Implement Services (`llm`, `chronos`, `paths`, `ledger`)
- [x] Action: 
  - Move `ollama_chat` from `chamber/council.rs` to `services::llm`. Add `embed(text)`.
  - Move Chronos `ArtifactOutcome`/client from `chamber/ritual.rs` to `services::chronos`.
  - Consolidate `app_data_root` into `services::paths`.
  - Implement an append-only, hash-chained JSONL ledger in `services::ledger` writing to `%LOCALAPPDATA%\NeuroCognica\Archetypes`.
  - Add tests for `app_data_root` and `ledger` hash chaining.
- Files touched: `crates/engine/src/services/mod.rs`, `crates/engine/src/services/llm.rs`, `crates/engine/src/services/chronos.rs`, `crates/engine/src/services/paths.rs`, `crates/engine/src/services/ledger.rs`, `crates/engine/src/chamber/council.rs`, `crates/engine/src/chamber/ritual.rs`.
- Expected outcome: Services are centralized, enabling multilane consumption.

### Step 4 — Shared Domain Types (`WitnessProfile`, `Difficulty`)
- [x] Action: Make `WitnessProfile` public, extending it with per-mode stats and an Arrow-II signal accumulator. Add a shared `Difficulty` type where difficulty is based on interpretive distance. Add tests for difficulty ordering.
- Files touched: `crates/engine/src/chamber/ritual.rs` (or where WitnessProfile lives), `crates/engine/src/modes/difficulty.rs`.
- Expected outcome: Domain primitives are ready for modes to consume.

### Step 5 — Canon Reconciliation (Codex and Viren)
- [x] Action: Update `theme_codex()` in `theme/constants.rs` to reflect the perfect fifth C-G ("Lexis"). Verify Viren Flamebearer and the Ember Covenant are preserved. Update any tests.
- Files touched: `crates/engine/src/theme/constants.rs`.
- Expected outcome: Archetype metadata matches the canon.

### Step 6 — Update Documentation
- [x] Action: Update `STATUS.md` and `README.md` to reflect the new architecture.
- Files touched: `STATUS.md`, `README.md` (if exists).
- Expected outcome: Documentation accurately describes the new Lane 0 spine.

### Step 7 — Verification and Final Push
- [x] Action: Run `cargo test --workspace`. If tests pass, commit and push to `origin/main`.
- Files touched: None (verification step).
- Expected outcome: A clean, tested, and published Lane 0 implementation.
