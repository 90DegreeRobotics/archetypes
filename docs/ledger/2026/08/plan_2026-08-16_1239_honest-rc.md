# Plan: Honest RC — 2026-08-16 12:39

## Status
COMPLETED

## Goal
Leave a clean `main` worktree, an honest 0.3.0 release candidate, proven Windows install/uninstall/Add-Remove Programs, Sentinel either strict-certified or claims downgraded to match the report, and fresh Desktop launch evidence. Dist remains gitignored. Origin/main is the audit surface after gates pass.

## Frozen rules
- Never delete. Never force-push. Never rebase or amend published history.
- Never present a stub as the real surface.
- Desktop click is the Founder path. Agents run CLI; the Founder does not.
- Sentinel `--strict` requires a clean git worktree. Certify after the RC commit.
- Do not mark Sentinel **certified** unless the harness and docs agree. Candidate is honest if key lifecycle is incomplete.
- Last state must leave a working Desktop Archetypes shortcut. Do not destroy Witness AppData (`-RemoveAppData` is forbidden in this unit).

## Surfaces
- **Upstream:** uncommitted 0.3.0 depth (TTS, hexagram, interiors, memory, sidecar launcher, Sentinel candidate, install scripts, docs).
- **Downstream:** Desktop `dist` restage, per-user/Program Files product install + ARP, Sentinel certify, launch veil → menu, STATUS/README/handoff.

## Steps
### Step 1 — Honest tree
- [x] Action: Inspect `git status` / `git diff` / `git log`. Stage product source only (crates, assets/help, scripts, docs, LICENSE, README, STATUS). Do not commit `dist/`, secrets, `.env`, AppData, or generated captures except evidence under `artifacts/visual-proof/honest-rc-*`.
- Files touched: working tree as listed in git status.
- Expected outcome: One why-focused RC commit on `main`.
- Result: `34619e4` shipped the 0.3.0 depth tree. Follow-up commit in this unit covers TTS crash fix, Director enforce sidecar flag, Sentinel PASS report, ARP/launch docs, and capture frames.

### Step 2 — Rust gate
- [x] Action: `cargo test --workspace` before calling the 0.3.0 tree honest.
- Files touched: none (verification).
- Expected outcome: Engine + launcher tests pass.
- Result: First gate 78 engine + 15 launcher. After TTS/enforce fixes: **79 engine + 16 launcher**, pass.

### Step 3 — Restage Desktop dist
- [x] Action: `pwsh -File scripts\install_shortcut.ps1` if binaries are stale vs source. Record PE FileVersion.
- Files touched: `dist/` (gitignored).
- Expected outcome: `dist\engine.exe` / `dist\launcher.exe` newer than source; Desktop shortcut target recorded.
- Result: Restaged 12:50. PE FileVersion **0.3.0.0**. Desktop → `C:\archetypes\dist\launcher.exe` cwd `C:\archetypes\dist`.

### Step 4 — Install / uninstall / ARP
- [x] Action: Run `install_product.ps1` (per-user if Program Files is not writable). Prove install root, HKCU/HKLM Uninstall\Archetypes (DisplayName, DisplayVersion, UninstallString, DisplayIcon), Desktop + Start Menu + Help shortcuts. Run `uninstall_product.ps1` without `-RemoveAppData`. Prove ARP + install-root binaries/shortcuts gone; AppData remains. Re-install so the Founder still has a working Desktop icon.
- Files touched: Windows install root, registry, shortcuts (not git).
- Expected outcome: Written proof in this plan and STATUS. Last state is a working game.
- Result:
  - Install root: `C:\Users\m\AppData\Local\Programs\Archetypes` (Program Files not writable). launcher.exe + engine.exe FileVersion 0.3.0.0.
  - HKCU `Software\Microsoft\Windows\CurrentVersion\Uninstall\Archetypes`: DisplayName=Archetypes, Publisher=NeuroCognica, DisplayVersion=0.3.0, InstallLocation=per-user root, DisplayIcon=...\archetypes.ico, UninstallString=pwsh ... uninstall_product.ps1. HKLM absent.
  - Shortcuts after install: Desktop + Start Menu Archetypes, Start Menu Help, Start Menu Uninstall.
  - Uninstall without `-RemoveAppData`: ARP gone, install root gone, all four shortcuts gone, AppData `C:\Users\m\AppData\Local\NeuroCognica\Archetypes` remained (data, logs, proof, sentinel).
  - Reinstalled product then restored Desktop to dist (AGENTS.md live surface). ARP still registered for the per-user copy.

### Step 5 — Sentinel certify or downgrade
- [x] Action: From `C:\sentinel-core`: `cargo run -p sentinel_cli --bin sentinel -- certify --repo C:\archetypes --product Archetypes --strict --output-dir C:\archetypes\docs\security`. If PASS, keep candidate unless harness says certified. If FAIL, downgrade README/STATUS/SENTINEL docs to match.
- Files touched: `docs/security/**`, possibly README/STATUS.
- Expected outcome: Docs match the report. No false certified/candidate claim.
- Result: **PASS**. Exact readiness line: `Certification readiness: candidate`. `adoption_readiness` PASS because the doc is marked candidate or certified. Not promoted to certified (key lifecycle + signing still open). Report `Result: PASS`, `strict_git_clean` PASS.

### Step 6 — Fresh launch evidence
- [x] Action: Launch staged `dist\launcher.exe` with `ARCHETYPES_LORE_CAPTURE` / `ARCHETYPES_CAPTURE_DIR` long enough for boot veil → menu. Save frames under `artifacts/visual-proof/honest-rc-2026-08-16_1239/`. Record last-engine.log, last-failure.txt absence/presence, PE versions.
- Files touched: capture dir, logs (AppData).
- Expected outcome: Visible proof of boot, not a silent close.
- Result:
  - First live Director was **shadow**; launcher fail-visible Notepad: `Chronos Sentinel authority is not enforcing (mode=shadow, enforced=Some(false))`. Proof: `00_fail_visible_shadow_mode.txt`. Restarted hotfix Director with `--sentinel-mode enforce`.
  - Next launch reached the window then **0xc0000005** (ORT API 27 vs sidecar 1.17.1) from in-process sherpa C API. Fail-visible. Proof: `01_engine_ort_capi_crash.txt`. Parked C API behind `ARCHETYPES_TTS_CAPI`; live path is CLI + WAV cache. Sidecar Director start now pins `--sentinel-mode enforce`.
  - After restage: capture exit 0, **last-failure.txt absent**, frames `00_title_arch.png`, `01_title_subtitle.png`, `02_lore_main_menu.png` (chamber menu, all five modes + HELP + QUIT, services up).

### Step 7 — Docs, commit remainder, push
- [x] Action: Update README, STATUS.md, CURRENT_HANDOFF.md, this plan. Commit report/doc/evidence updates. `git push origin main` when the tree is clean and gates passed.
- Files touched: docs listed above.
- Expected outcome: Clean `main` on origin. Founder next step is click Desktop Archetypes.

## Verification record
- `cargo test --workspace`: 79 engine + 16 launcher, pass.
- Sentinel: `Result: PASS` / `Certification readiness: candidate`.
- ARP: HKCU Uninstall\Archetypes DisplayVersion 0.3.0; uninstall kept AppData.
- Live surface: Desktop `Archetypes.lnk` → `C:\archetypes\dist\launcher.exe`.
- Evidence: `artifacts/visual-proof/honest-rc-2026-08-16_1239/`.
