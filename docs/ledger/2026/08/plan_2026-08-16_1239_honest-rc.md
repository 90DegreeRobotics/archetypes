# Plan: Honest RC — 2026-08-16 12:39

## Status
IN-PROGRESS

## Goal
Leave a clean `main` worktree, an honest 0.3.0 release candidate, proven Windows install/uninstall/Add-Remove Programs, Sentinel either strict-certified or claims downgraded to match the report, and fresh Desktop launch evidence. Dist remains gitignored. Origin/main is the audit surface after gates pass.

## Frozen rules
- Never delete. Never force-push. Never rebase or amend published history.
- Never present a stub as the real surface.
- Desktop click is the Founder path. Agents run CLI; the Founder does not.
- Sentinel `--strict` requires a clean git worktree. Certify after the RC commit.
- Do not mark Sentinel **certified** unless the harness and docs agree. Candidate is honest if key lifecycle is incomplete.
- Last state must leave a working Desktop Archetypes shortcut. Do not destroy Witness AppData (`-RemoveAppData` is forbidden in this unit).

## Surfaces
- **Upstream:** uncommitted 0.3.0 depth (TTS, hexagram, interiors, memory, sidecar launcher, Sentinel candidate, install scripts, docs).
- **Downstream:** Desktop `dist` restage, per-user/Program Files product install + ARP, Sentinel certify, launch veil → menu, STATUS/README/handoff.

## Steps
### Step 1 — Honest tree
- [ ] Action: Inspect `git status` / `git diff` / `git log`. Stage product source only (crates, assets/help, scripts, docs, LICENSE, README, STATUS). Do not commit `dist/`, secrets, `.env`, AppData, or generated captures except evidence under `artifacts/visual-proof/honest-rc-*`.
- Files touched: working tree as listed in git status.
- Expected outcome: One why-focused RC commit on `main`.

### Step 2 — Rust gate
- [ ] Action: `cargo test --workspace` before calling the 0.3.0 tree honest.
- Files touched: none (verification).
- Expected outcome: Engine + launcher tests pass.

### Step 3 — Restage Desktop dist
- [ ] Action: `pwsh -File scripts\install_shortcut.ps1` if binaries are stale vs source. Record PE FileVersion.
- Files touched: `dist/` (gitignored).
- Expected outcome: `dist\engine.exe` / `dist\launcher.exe` newer than source; Desktop shortcut target recorded.

### Step 4 — Install / uninstall / ARP
- [ ] Action: Run `install_product.ps1` (per-user if Program Files is not writable). Prove install root, HKCU/HKLM Uninstall\Archetypes (DisplayName, DisplayVersion, UninstallString, DisplayIcon), Desktop + Start Menu + Help shortcuts. Run `uninstall_product.ps1` without `-RemoveAppData`. Prove ARP + install-root binaries/shortcuts gone; AppData remains. Re-install so the Founder still has a working Desktop icon.
- Files touched: Windows install root, registry, shortcuts (not git).
- Expected outcome: Written proof in this plan and STATUS. Last state is a working game.

### Step 5 — Sentinel certify or downgrade
- [ ] Action: From `C:\sentinel-core`: `cargo run -p sentinel_cli --bin sentinel -- certify --repo C:\archetypes --product Archetypes --strict --output-dir C:\archetypes\docs\security`. If PASS, keep candidate unless harness says certified. If FAIL, downgrade README/STATUS/SENTINEL docs to match.
- Files touched: `docs/security/**`, possibly README/STATUS.
- Expected outcome: Docs match the report. No false certified/candidate claim.

### Step 6 — Fresh launch evidence
- [ ] Action: Launch staged `dist\launcher.exe` with `ARCHETYPES_LORE_CAPTURE` / `ARCHETYPES_CAPTURE_DIR` long enough for boot veil → menu. Save frames under `artifacts/visual-proof/honest-rc-2026-08-16_1239/`. Record last-engine.log, last-failure.txt absence/presence, PE versions.
- Files touched: capture dir, logs (AppData).
- Expected outcome: Visible proof of boot, not a silent close.

### Step 7 — Docs, commit remainder, push
- [ ] Action: Update README, STATUS.md, CURRENT_HANDOFF.md, this plan. Commit report/doc/evidence updates. `git push origin main` when the tree is clean and gates passed.
- Files touched: docs listed above.
- Expected outcome: Clean `main` on origin. Founder next step is click Desktop Archetypes.

## Verification record
(filled as the unit runs)
