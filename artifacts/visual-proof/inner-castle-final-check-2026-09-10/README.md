# Final-check capture attempt — 2026-09-10

The installed `engine.exe` was started with `ARCHETYPES_INNER_CAPTURE=1` and this
directory as `ARCHETYPES_CAPTURE_DIR`. It exited without writing PNG frames. This
directory is retained as an honest record of that failed capture attempt; it is not
visual proof. The installed Desktop/Taskbar staging and automated test gates did pass.
