# Final-check capture attempt — 2026-09-10

The installed `engine.exe` was started with `ARCHETYPES_INNER_CAPTURE=1` and this
directory as `ARCHETYPES_CAPTURE_DIR`. The process returned before asynchronous PNG
encoding completed, so an immediate directory listing was empty; the eight numbered
frames subsequently appeared and are the rendered-frame witness for this installed build.
The installed Desktop/Taskbar staging and automated test gates also passed.
