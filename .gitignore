/target

# Runtime-staged Chronos renders returned into the chamber (not source assets)
/assets/artifacts/

# Deterministic visual-capture scratch runs. Accepted review frames are force-added.
/artifacts/visual-proof/council-text-ui*
/artifacts/visual-proof/portal-table-final*
/artifacts/visual-proof/locked-choreography*

# Release staging produced by scripts/install_shortcut.ps1
/dist/

# Signing & Installer outputs
/tools/signing/
/installer/output/
installer/signing/azure-metadata.json
/Archetypes_Setup_*.exe

# Quality-lab working set: regenerated by scripts/manifest_quality_lab.py.
# The bundles and intermediate GLBs are large and reproducible; the contact
# sheets that carry the findings are committed under artifacts/visual-proof.
artifacts/quality-lab/bundles/
artifacts/quality-lab/glb/
artifacts/quality-lab/renders/
